#!/bin/dash
checkoutMain () {
    for name in "$@"; do
        git rev-parse --verify $name >/dev/null 2>/dev/null && git checkout $name && exit
    done
}

checkoutMain "main" "master" "mainline" "default"